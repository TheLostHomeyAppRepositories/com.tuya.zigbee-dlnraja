<<<<<<< HEAD
name: Backup mensuel Google Drive
=======
name: 💾 Monthly Backup Optimisé
>>>>>>> master

on:
  schedule:
    - cron: '0 4 1 * *'
<<<<<<< HEAD

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Installer rclone
        run: curl https://rclone.org/install.sh | sudo bash
      - name: Archiver le repo
        run: zip -r tuya-backup-$(date +%Y%m%d).zip .
      - name: Uploader sur Google Drive
        env:
          RCLONE_CONFIG: ${{ secrets.RCLONE_CONFIG }}
        run: rclone copy tuya-backup-$(date +%Y%m%d).zip gdrive:com.tuya.zigbee/backups/
=======
  workflow_dispatch:

jobs:
  monthly-backup:
    runs-on: ubuntu-latest
    name: 💾 Sauvegarde mensuelle robuste
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
      - name: 💾 Création de l'archive de sauvegarde
        run: |
          echo "💾 Création de l'archive de sauvegarde..."
          tar -czf tuya-backup-$(date +%Y-%m-%d).tar.gz --exclude="node_modules" --exclude=".git" --exclude="*.log" .
          echo "✅ Archive créée."
      - name: 📤 Upload de la sauvegarde
        uses: actions/upload-artifact@v4
        with:
          name: tuya-backup
          path: tuya-backup-*.tar.gz
      - name: 📈 Rapport de sauvegarde
        run: |
          echo "📈 Rapport de sauvegarde : archive générée et uploadée."
          echo "✅ Sauvegarde mensuelle terminée avec succès."
>>>>>>> master
